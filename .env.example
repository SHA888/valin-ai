# Vite environment variables (exposed to the browser)
# Copy to .env or .env.local and fill with your values

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Optional: choose primary data provider for client flows
# Allowed: yahoo | fmp | alpha_vantage | bloomberg | mock
VITE_DATA_PROVIDER_PRIMARY=yahoo

# NOTE: Do NOT place real provider API keys in Vite envs (they are exposed to the browser).
# Put provider secrets in your server/edge function environment instead.
# The following are listed here for reference/documentation only.

# --- Server/Edge Function Environment (examples; do NOT use in client) ---
# FMP_API_KEY=your-fmp-key
# ALPHA_VANTAGE_API_KEY=your-av-key
# NEWS_API_KEY=your-newsapi-key
#
# Bloomberg (BLPAPI) typically requires enterprise setup and certificates.
# These placeholders are for server-side configuration only.
# BLOOMBERG_ENABLED=false
# BLOOMBERG_APP_NAME=your-app-name
# BLOOMBERG_API_HOST=localhost
# BLOOMBERG_API_PORT=8194
